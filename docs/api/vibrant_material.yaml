name: Vibrant::Material
platform: ['macOS']
header: nativeui/vibrant.h
type: enum class
namespace: nu
description: The material of a `Vibrant` view.

detail: |
  This type can have following values:
  * `<!enum class>Vibrant::Material::AppearanceBased`
  * `<!enum class>Vibrant::Material::Light`
  * `<!enum class>Vibrant::Material::Dark`
  * `<!enum class>Vibrant::Material::Titlebar`
