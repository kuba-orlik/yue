name: Clipboard::Data::Type
header: nativeui/clipboard.h
type: enum class
namespace: nu
description: Type of clipboard data.

detail: |
  This type can have following values:
  * `<!enum class>Clipboard::Data::Type::Text`
  * `<!enum class>Clipboard::Data::Type::HTML`
  * `<!enum class>Clipboard::Data::Type::Image`
  * `<!enum class>Clipboard::Data::Type::FilePaths`
  * `<!enum class>Clipboard::Data::Type::None`
