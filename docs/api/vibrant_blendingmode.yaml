name: Vibrant::BlendingMode
platform: ['macOS']
header: nativeui/vibrant.h
type: enum class
namespace: nu
description: The blending mode of a `Vibrant` view.

detail: |
  This type can have following values:
  * `<!enum class>Vibrant::BlendingMode::BehindWindow`
  * `<!enum class>Vibrant::BlendingMode::WithinWindow`
