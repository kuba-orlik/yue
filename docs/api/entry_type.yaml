name: Entry::Type
header: nativeui/entry.h
type: enum class
namespace: nu
description: Represent possible `Entry` types.

detail: |
  This type can have following values:
  * `!<enum class>Entry::Type::Normal`
  * `!<enum class>Entry::Type::Password`
