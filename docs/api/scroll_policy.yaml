name: Scroll::Policy
header: nativeui/scroll.h
type: enum class
namespace: nu
description: Scrollbar policy.

detail: |
  This type can have following values:
  * `<!enum class>Scroll::Policy::Always`
  * `<!enum class>Scroll::Policy::Never`
  * `<!enum class>Scroll::Policy::Automatic`
