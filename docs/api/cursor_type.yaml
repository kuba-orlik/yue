name: Cursor::Type
header: nativeui/cursor.h
type: enum class
namespace: nu
description: Type of cursor.

detail: |
  This type can have following values:
  * `<!enum class>Cursor::Type::Default`
  * `<!enum class>Cursor::Type::Hand`
  * `<!enum class>Cursor::Type::Crosshair`
  * `<!enum class>Cursor::Type::Progress`
  * `<!enum class>Cursor::Type::Text`
  * `<!enum class>Cursor::Type::NotAllowed`
  * `<!enum class>Cursor::Type::Help`
  * `<!enum class>Cursor::Type::Move`
  * `<!enum class>Cursor::Type::ResizeEW`
  * `<!enum class>Cursor::Type::ResizeNS`
  * `<!enum class>Cursor::Type::ResizeNESW`
  * `<!enum class>Cursor::Type::ResizeNWSE`
