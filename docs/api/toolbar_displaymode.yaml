name: Toolbar::DisplayMode
header: nativeui/toolbar.h
type: enum class
namespace: nu
description: Possible display modes of `Toolbar`.

detail: |
  This type can have following values:
  * `<!enum class>Toolbar::DisplayMode::Default`
  * `<!enum class>Toolbar::DisplayMode::IconAndLabel`
  * `<!enum class>Toolbar::DisplayMode::Icon`
  * `<!enum class>Toolbar::DisplayMode::Label`
