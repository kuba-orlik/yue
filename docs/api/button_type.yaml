name: Button::Type
header: nativeui/button.h
type: enum class
namespace: nu
description: Represent possible `Button` types.

detail: |
  This type can have following values:
  * `<!enum class>Button::Type::Normal`
  * `<!enum class>Button::Type::Checkbox`
  * `<!enum class>Button::Type::Radio`
