name: System
component: gui
header: nativeui/system.h
type: class
namespace: nu
description: Get information about system settings.

class_methods:
  - signature: Color GetColor(System::Color name)
    description: Return the theme color with `name`.
