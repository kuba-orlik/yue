name: TextAlign
header: nativeui/gfx/text.h
type: enum class
namespace: nu
description: Text alignment.

detail: |
  This type can have following values:
  * `<!enum class>TextAlign::Start`
  * `<!enum class>TextAlign::Center`
  * `<!enum class>TextAlign::End`
