name: EventType
lang: ['cpp']
header: nativeui/events/event.h
type: enum class
namespace: nu
description: Represent possible event types.

detail: |
  This type can have following values:
  * `<!enum class>EventType::Unknown`
  * `<!enum class>EventType::MouseDown`
  * `<!enum class>EventType::MouseUp`
  * `<!enum class>EventType::MouseMove`
  * `<!enum class>EventType::MouseEnter`
  * `<!enum class>EventType::MouseLeave`
  * `<!enum class>EventType::KeyDown`
  * `<!enum class>EventType::KeyUp`
