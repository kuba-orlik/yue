name: Font::Style
header: nativeui/gfx/font.h
type: enum class
namespace: nu
description: Represent font styles.

detail: |
  This type can have following values:
  * `<!enum class>Font::Style::Normal`
  * `<!enum class>Font::Style::Italic`
