name: System::Color
header: nativeui/system.h
type: enum class
namespace: nu
description: System theme colors.

lang_detail:
  cpp: |
    This type is an `enum class` with following values:
      * `System::Color::Text`
      * `System::Color::DisabledText`

  lua: &ref |
    This type is a string with following possible values:
    * `"text"`
    * `"disabled-text"`

  js: *ref
