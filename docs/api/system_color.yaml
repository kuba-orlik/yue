name: System::Color
header: nativeui/system.h
type: enum class
namespace: nu
description: System theme colors.

detail: |
  This type can have following values:
  * `<!enum class>System::Color::Text` - Normal text color.
  * `<!enum class>System::Color::DisabledText` - Text color for disabled views.
